{
    "bind_addr": "0.0.0.0",
    "addresses": {
        "http": "0.0.0.0"
    },
    "advertise_addr": "{{ansible_default_ipv4.address}}",
    "server": true,
    "ui": true,
    "data_dir": "/opt/consul",
    "bootstrap_expect": 1,
    "acl_datacenter": "dc1",
    "acl_default_policy": "allow",
    "acl_down_policy": "extend-cache",
    "acl_master_token": "{{ ansible_hostname | to_uuid }}"
}